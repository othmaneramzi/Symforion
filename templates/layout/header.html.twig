<nav class="navbar navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href=" {{ url('home') }}">
            <img src="{{ asset('img/logo_salmon.svg') }}" alt="" width="30" height="24" class="d-inline-block align-text-top">
            Symforion
        </a>
        {% if app.user %}
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item">
                    {% if is_granted("ROLE_TEACHER") %} <a class="nav-link" aria-current="page" href="{{ url('manage_marks') }}">Ajouter une note</a>{% endif %}
                    {% if is_granted("ROLE_STUDENT") %} <a class="nav-link" aria-current="page" href="{{ url('display_marks') }}">Afficher mes notes</a>{% endif %}
                  </li>
              </ul>
        

            <span class="navbar-text">
                {{ app.user.firstname }} {{ app.user.lastname }}
                <a href="{{ url('app_logout') }}" class="ms-2"><button type="button" class="btn btn-outline-salmon rounded-pill">Déconnexion</button></a>
            </span>
        {% endif %}
    </div>
</nav>